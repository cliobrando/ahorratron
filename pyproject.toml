[project]
name = "ahorratron"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.0",
]

[project.scripts]
convert-to-actual = "ahorratron.cli.convert_to_actual:main"

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
